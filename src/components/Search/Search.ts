import { styled, alpha } from "@mui/material/styles";
import React from "react";

const Search: React.FC = styled("div")(({}) => ({
  position: "relative",
  borderRadius: 20,
  marginLeft: 0,
  width: "100%",
}));

export default Search;
